Use login credentials for admin user:
    username: rskeeter
    password: password

Use login credentials for regular volunteer (non-admin)
    username: rweasley
    password: password

Note on logging in: you may login with any username in the database (Volunteer table). The password for each volunteer is password
You may choose to change the password to something else within the application. Passwords are stored after being cryptographically hashed
(ie not the original text).

Ensure that all database tables have been committed prior to using the application for best demo results.

Package name is FinalProject.

database connection within Oracle SQL command line:
javauser
javapass